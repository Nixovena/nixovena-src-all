#!/bin/bash

# set -e

# echo "Fixing permissions..."

chmod +x /usr/bin/liveboot
chmod +x /usr/bin/conky-manager
chmod +x /usr/bin/conky-reanchor
chmod +x /usr/bin/overkill-setup
chmod +x /usr/bin/overkill-setup-cli

# echo "Permissions fixed!"