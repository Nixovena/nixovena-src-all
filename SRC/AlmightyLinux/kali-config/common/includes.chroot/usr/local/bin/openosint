#!/usr/bin/env python3
import sys

try:
    import openosint.cli
    openosint.cli.main()
except ImportError:
    print("Error: openosint is not installed. Run 'pip install openosint'.")
    sys.exit(1)